"""PostgreSQL repository adapters for production deployments.

These adapters are intentionally not wired into the local demo runtime yet.
SQLite remains the default backend until the production adapter is completed
and tested against a real PostgreSQL service.
"""

